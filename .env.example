# Database (SQLite default fallback for local dev; set PostgreSQL URL if available)
DATABASE_URL=sqlite:///./skillbridge.db
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/skillbridge_db

# Gemini AI (Optional - System uses deterministic fallback engine if blank)
GEMINI_API_KEY=

# JWT Secret
JWT_SECRET=skillbridge_super_secret_jwt_key_2026_sih

# Frontend URL
VITE_API_URL=/api
